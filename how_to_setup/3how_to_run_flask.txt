To run flask type:

flask run

========================================================================

If you get "this address is already in use" use:

flask run --port=X       (where X is a value, not 5000, e.g. 4999)

========================================================================

Don't forget:

(if in lab)
source ~/flask/bin/activate

export FLASK_APP=run.py
export FLASK_ENV=development

flask run
